{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "watch": "cd Frontend && yarn watch",
        "build": "cd Frontend && yarn build",
        "backend": "dotnet watch run"
    },
    "devDependencies": {
        "@types/react": "^18.3.12",
        "@types/react-dom": "^18.3.1",
        "typescript": "^5.6.3"
    },
    "dependencies": {
        "react-router-dom": "^6.27.0"
    }
}
